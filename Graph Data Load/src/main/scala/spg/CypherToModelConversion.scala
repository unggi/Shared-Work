package spg

import java.io.{File, FileReader, LineNumberReader}

object CypherToModelConversion {

  val INPUT_FILE = "src/main/scala/puml/Cypher.sql"

  def main(arg: Array[String]): Unit = {

//    println("Generating UML model from Cypher input code.")
//    println()
//
//    val input = new File(INPUT_FILE)
//    val reader = new LineNumberReader(new FileReader(input))
//
//    var eof = false
//    continuously (reader.readLine) {
//      case null => eof = true
//      case line: String if line.length > 0 =>
//        // Process this line
//      case s =>
//        eof = true
//    }
//  }

}
